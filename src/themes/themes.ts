import { Theme } from '../utils/setTheme';

const themes: { [key: string]: Theme } = {
  default: {
    textColor: 'c3d1d9',
    bgColor: '0d1117',
    accentColor: '58a6ff',
    borderColor: 'c3d1d9',
  },
};

export { themes };
